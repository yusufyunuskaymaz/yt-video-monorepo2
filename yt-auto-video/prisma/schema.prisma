generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Project {
  id            Int         @id @default(autoincrement())
  title         String
  totalDuration Int
  totalScenes   Int
  status        String      @default("pending")
  defaultStyle  String?
  finalVideoUrl String?
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  scenes        Scene[]
  characters    Character[]
}

model Scene {
  id               Int      @id @default(autoincrement())
  projectId        Int
  project          Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  sceneNumber      Int
  timestamp        String
  narration        String
  subject          String
  characters       String? // JSON array: ["Sultan", "Vezir"]
  isDialog         Boolean  @default(false)
  speechText       String? // Dialog konuşma metni — ElevenLabs'a bu gönderilir
  usePrevRef       Boolean  @default(false)
  videoPrompt      String?
  imageUrl         String?
  audioUrl         String?
  audioDuration    Float?
  audioVoice       String?
  audioTemperature Float?
  videoUrl         String?
  mergedVideoUrl   String?
  dubbedVideoUrl   String?
  generationLog    String? // JSON: prompt, refs, model, duration
  status           String   @default("pending")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([projectId])
  @@index([status])
}

model Character {
  id          Int      @id @default(autoincrement())
  projectId   Int?
  project     Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)
  name        String
  description String?
  imageUrl    String
  createdAt   DateTime @default(now())

  @@index([projectId])
}
